APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# Turns resolve the moment they are submitted. This is only the recovery
# window for a turn whose request died mid-resolution — leave it alone unless
# resolutions here honestly take longer than two minutes.
GAME_ABANDONED_TURN_MINUTES=2
# World evolution: daily|weekly|off, and the local hour its chronicle push
# lands. SET THE TIMEZONE — the scheduler otherwise uses APP_TIMEZONE (UTC),
# and "The world changed overnight" arrives in the middle of someone's evening.
GAME_EVOLVE_SCHEDULE=daily
GAME_EVOLVE_AT=07:30
GAME_SCHEDULE_TIMEZONE=UTC
CLAUDE_BINARY=claude
CLAUDE_MODEL=claude-sonnet-5
# On the server: HOME of the user that ran `claude setup-token`, so php-fpm/cron
# invocations can find ~/.claude auth (e.g. /home/gemini). Leave empty locally.
CLAUDE_HOME=
# Long-lived subscription token printed by `claude setup-token` (sk-ant-oat01-…),
# if using token auth instead of on-disk ~/.claude credentials. Leave empty locally.
CLAUDE_OAUTH_TOKEN=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=
